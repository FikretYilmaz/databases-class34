1.) food_code and food description have more than one value
dinner_date has different type of date
2.) Column about Dinner venue and food could be separate
3.) First Table(member) : member_id(PK), member_name,member_address
    Second Table(dinner): dinner_id, dinner_date,venues_code(FK)
    Third Table(venue) : venues_code, venue_description
    Fourth Table(food): food_code(PK), food_description
    Fifth Table(order) :id(PK),food_code(FK),member_id(FK),dinner_id(FK),quantity,